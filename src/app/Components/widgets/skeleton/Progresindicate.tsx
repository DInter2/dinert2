'use client'

import { CircularProgress, Box } from '@mui/material';
import React from 'react'

const Progresindicate = () => {
  return <CircularProgress color='warning' size={60}/>;
}

export default Progresindicate
